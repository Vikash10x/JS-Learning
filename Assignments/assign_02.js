// ------------- Q1 -------------

// function greet(user) {
//   console.log("Hi " + user.name + " your age is " + user.age);
// }

// let user = {
//   name: "vikash",
//   age: 19,
// };

// greet(user);

// ---------- Q2 -----------

function greet(user) {
  console.log("hi " + "Mr." + user.name + " your age is " + user.age);
}

let user = {
  name: "vikash",
  age: 21,
  gender: "male",
};

greet(user);

let arr = ["vikash", 21, true];
console.log(typeof arr[2]);
